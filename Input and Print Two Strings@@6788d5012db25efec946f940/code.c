#include <stdio.h>

int main() {
    char word1[50], word2[50];

    // Taking two words as input
    scanf("%s %s", word1, word2);

    // Printing the output as per the requirement
    printf("You entered: %s and %s\n", word1, word2);

    return 0;
}
